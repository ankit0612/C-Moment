#include<stdio.h>
 int main()
  {
      int l,b,area;
      
      printf("Enter the length and breath of the Rectangle\n\n");
      scanf("%d%d",&l,&b);
      area=l*b;
      
      printf("Area of the Rectangle\n\n%d",area);
      return 0;
  }
